name: gpt_markdown
description: Lightweight local fallback for GPT markdown rendering
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: false
